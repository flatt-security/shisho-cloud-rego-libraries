# - This code was generated by Shisho Cloud's internal system (protoc-gen-regolib).
# - When you find something to improve, you can create an GitHub issue, instead of creating pull requests.

package shisho.decision.aws.cognito

import data.shisho
import data.shisho.assertion
import data.shisho.primitive

import future.keywords.every

# @title Ensure That the Web Application Minimizes Role Assignment to Authenticated Users of Cognito Identity Pool
# You can emit this decision as follows:
#
# ```
# import data.shisho
#
# decisions[d] {
#   # the resource ID to review (e.g. a GitHub repository, etc.)
#   subject := "test"
#
#   # whether the target is allowed by this policy or not
#   allowed := true
#
#   d := shisho.decision.aws.cognito.authenticated_role({
#     "allowed": allowed,
#     "subject": subject,
#     "payload": shisho.decision.aws.cognito.authenticated_role_payload({
#     }),
#   })
# }
# ```

# METADATA
# title: "decision.api.shisho.dev/v1beta:aws_cognito_authenticated_role"
# scope: "rule"
# description: |
#   Emits a decision whose type is decision.api.shisho.dev/v1beta:aws_cognito_authenticated_role".
authenticated_role(d) = x {
	shisho.decision.has_required_fields(d)
	x := {
		"header": authenticated_role_header({
			"allowed": d.allowed,
			"subject": d.subject,
			"locator": authenticated_role_locator(d),
			"severity": authenticated_role_severity(d),
		}),
		"payload": d.payload,
	}
}

authenticated_role_severity(d) := shisho.decision.severity_info {
	d.allowed == true
} else := d.severity {
	not is_null(d.severity)
} else := 3

authenticated_role_locator(d) := d.locator {
	not is_null(d.locator)
} else := ""

authenticated_role_kind = "aws_cognito_authenticated_role"

authenticated_role_header(h) = x {
	x := {
		"api_version": "decision.api.shisho.dev/v1beta",
		"kind": authenticated_role_kind,
		"subject": h.subject,
		"locator": h.locator,
		"severity": h.severity,
		"labels": {},
		"annotations": {"decision.api.shisho.dev:ssc/category": "infrastructure"},
		"type": shisho.decision.as_decision_type(authenticated_role_allowed(h)),
	}
}

# Force to allow the given decision following resource exception policy
authenticated_role_allowed(h) {
	data.params != null
	data.params.resource_exceptions != null
	shisho.resource.is_excepted(data.params.resource_exceptions, h.subject)
} else := h.allowed

# METADATA
# title: "Entry of decision.api.shisho.dev/v1beta:aws_cognito_authenticated_role"
# scope: "rule"
# description: |
#   Emits a decision entry describing the detail of a decision decision.api.shisho.dev/v1beta:aws_cognito_authenticated_role
#
#   The parameter `data` shoud be empty object (`{}`).
authenticated_role_payload(edata) := x {
	authenticated_role_payload_assert(edata, "<the argument to authenticated_role_payload>")
	x := json.marshal(edata)
} else := ""

authenticated_role_payload_assert(edata, hint) {
	assertion.is_type(edata, "object", hint)

	key_checks := []
	every c in key_checks { c }

	value_checks := []
	every c in value_checks { c }
} else := false

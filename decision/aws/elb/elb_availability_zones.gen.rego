# - This code was generated by Shisho Cloud's internal system (protoc-gen-regolib).
# - When you find something to improve, you can create an GitHub issue, instead of creating pull requests.

package shisho.decision.aws.elb

import data.shisho
import data.shisho.assertion
import data.shisho.primitive

import future.keywords.every

# @title Ensure that AWS Load Balancers span multiple Availability Zones
# You can emit this decision as follows:
#
# ```
# import data.shisho
#
# decisions[d] {
#   # the resource ID to review (e.g. a GitHub repository, etc.)
#   subject := "test"
#
#   # whether the target is allowed by this policy or not
#   allowed := true
#
#   d := shisho.decision.aws.elb.availability_zones({
#     "allowed": allowed,
#     "subject": subject,
#     "payload": shisho.decision.aws.elb.availability_zones_payload({
#       "zones": ["example"],
#     }),
#   })
# }
# ```

# METADATA
# title: "decision.api.shisho.dev/v1beta:aws_elb_availability_zones"
# scope: "rule"
# description: |
#   Emits a decision whose type is decision.api.shisho.dev/v1beta:aws_elb_availability_zones".
availability_zones(d) = x {
	shisho.decision.has_required_fields(d)
	x := {
		"header": availability_zones_header({
			"allowed": d.allowed,
			"subject": d.subject,
			"locator": availability_zones_locator(d),
			"severity": availability_zones_severity(d),
		}),
		"payload": d.payload,
	}
}

availability_zones_severity(d) := shisho.decision.severity_info {
	d.allowed == true
} else := d.severity {
	not is_null(d.severity)
} else := 1

availability_zones_locator(d) := d.locator {
	not is_null(d.locator)
} else := ""

availability_zones_kind = "aws_elb_availability_zones"

availability_zones_header(h) = x {
	x := {
		"api_version": "decision.api.shisho.dev/v1beta",
		"kind": availability_zones_kind,
		"subject": h.subject,
		"locator": h.locator,
		"severity": h.severity,
		"labels": {},
		"annotations": {
			"decision.api.shisho.dev:aws/fsbp/latest": "ELB.13",
			"decision.api.shisho.dev:needs-manual-review": "false",
			"decision.api.shisho.dev:ssc/category": "infrastructure",
		},
		"type": shisho.decision.as_decision_type(availability_zones_allowed(h)),
	}
}

# Force to allow the given decision following resource exception policy
availability_zones_allowed(h) {
	data.params != null
	data.params.resource_exceptions != null
	shisho.resource.is_excepted(data.params.resource_exceptions, h.subject)
} else := h.allowed

# METADATA
# title: "Entry of decision.api.shisho.dev/v1beta:aws_elb_availability_zones"
# scope: "rule"
# description: |
#   Emits a decision entry describing the detail of a decision decision.api.shisho.dev/v1beta:aws_elb_availability_zones
#
#   The parameter `data` is an object with the following fields:
#   - zones: string
#
#   For instance, `data` can take the following value:
#   ```rego
#   {
#     "zones": ["example"],
#   }
#   ```
availability_zones_payload(edata) := x {
	availability_zones_payload_assert(edata, "<the argument to availability_zones_payload>")
	x := json.marshal(edata)
} else := ""

availability_zones_payload_assert(edata, hint) {
	assertion.is_type(edata, "object", hint)

	key_checks := [assertion.has_key(edata, "zones", concat("", [hint, ".", "zones"]))]
	every c in key_checks { c }

	value_checks := [availability_zones_payload_assert_zones(edata, "zones", concat("", [hint, ".", "zones"]))]
	every c in value_checks { c }
} else := false

availability_zones_payload_assert_zones(x, key, hint) {
	assertion.is_set_or_array(x[key], hint)
	checks := [availability_zones_payload_assert_zones_element(x[key], i, concat("", [
		hint,
		"[",
		format_int(i, 10),
		"]",
	])) |
		_ := x[key][i]
	]
	every c in checks { c }
} else := false

availability_zones_payload_assert_zones_element(x, key, hint) {
	assertion.is_type(x[key], "string", hint)
} else := false

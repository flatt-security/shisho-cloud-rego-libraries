# - This code was generated by Shisho Cloud's internal system (protoc-gen-regolib).
# - When you find something to improve, you can create an GitHub issue, instead of creating pull requests.

package shisho.decision.aws.elb

import data.shisho
import data.shisho.assertion
import data.shisho.primitive

import future.keywords.every

# @title Ensure that the Web Application Enforces the Use of HTTPS When Connecting to ELB
# You can emit this decision as follows:
#
# ```
# import data.shisho
#
# decisions[d] {
#   # the resource ID to review (e.g. a GitHub repository, etc.)
#   subject := "test"
#
#   # whether the target is allowed by this policy or not
#   allowed := true
#
#   d := shisho.decision.aws.elb.transport({
#     "allowed": allowed,
#     "subject": subject,
#     "payload": shisho.decision.aws.elb.transport_payload({
#     }),
#   })
# }
# ```

# METADATA
# title: "decision.api.shisho.dev/v1beta:aws_elb_transport"
# scope: "rule"
# description: |
#   Emits a decision whose type is decision.api.shisho.dev/v1beta:aws_elb_transport".
transport(d) = x {
	shisho.decision.has_required_fields(d)
	x := {
		"header": transport_header({
			"allowed": d.allowed,
			"subject": d.subject,
			"locator": transport_locator(d),
			"severity": transport_severity(d),
		}),
		"payload": d.payload,
	}
}

transport_severity(d) := shisho.decision.severity_info {
	d.allowed == true
} else := d.severity {
	not is_null(d.severity)
} else := 3

transport_locator(d) := d.locator {
	not is_null(d.locator)
} else := ""

transport_kind = "aws_elb_transport"

transport_header(h) = x {
	x := {
		"api_version": "decision.api.shisho.dev/v1beta",
		"kind": transport_kind,
		"subject": h.subject,
		"locator": h.locator,
		"severity": h.severity,
		"labels": {},
		"annotations": {"decision.api.shisho.dev:ssc/category": "infrastructure"},
		"type": shisho.decision.as_decision_type(transport_allowed(h)),
	}
}

# Force to allow the given decision following resource exception policy
transport_allowed(h) {
	data.params != null
	data.params.resource_exceptions != null
	shisho.resource.is_excepted(data.params.resource_exceptions, h.subject)
} else := h.allowed

# METADATA
# title: "Entry of decision.api.shisho.dev/v1beta:aws_elb_transport"
# scope: "rule"
# description: |
#   Emits a decision entry describing the detail of a decision decision.api.shisho.dev/v1beta:aws_elb_transport
#
#   The parameter `data` shoud be empty object (`{}`).
transport_payload(edata) := x {
	transport_payload_assert(edata, "<the argument to transport_payload>")
	x := json.marshal(edata)
} else := ""

transport_payload_assert(edata, hint) {
	assertion.is_type(edata, "object", hint)

	key_checks := []
	every c in key_checks { c }

	value_checks := []
	every c in value_checks { c }
} else := false

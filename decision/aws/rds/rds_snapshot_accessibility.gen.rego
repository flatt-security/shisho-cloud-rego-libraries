# - This code was generated by Shisho Cloud's internal system (protoc-gen-regolib).
# - When you find something to improve, you can create an GitHub issue, instead of creating pull requests.

package shisho.decision.aws.rds

import data.shisho
import data.shisho.assertion
import data.shisho.primitive

import future.keywords.every

# @title Ensure that RDS snapshot is private
# You can emit this decision as follows:
#
# ```
# import data.shisho
#
# decisions[d] {
#   # the resource ID to review (e.g. a GitHub repository, etc.)
#   subject := "test"
#
#   # whether the target is allowed by this policy or not
#   allowed := true
#
#   d := shisho.decision.aws.rds.snapshot_accessibility({
#     "allowed": allowed,
#     "subject": subject,
#     "payload": shisho.decision.aws.rds.snapshot_accessibility_payload({
#       "engine": "example",
#       "snapshots": [{"id": "example", "publicly_restorable": false}],
#     }),
#   })
# }
# ```

# METADATA
# title: "decision.api.shisho.dev/v1beta:aws_rds_snapshot_accessibility"
# scope: "rule"
# description: |
#   Emits a decision whose type is decision.api.shisho.dev/v1beta:aws_rds_snapshot_accessibility".
snapshot_accessibility(d) = x {
	shisho.decision.has_required_fields(d)
	x := {
		"header": snapshot_accessibility_header({
			"allowed": d.allowed,
			"subject": d.subject,
			"locator": snapshot_accessibility_locator(d),
			"severity": snapshot_accessibility_severity(d),
		}),
		"payload": d.payload,
	}
}

snapshot_accessibility_severity(d) := shisho.decision.severity_info {
	d.allowed == true
} else := d.severity {
	not is_null(d.severity)
} else := 4

snapshot_accessibility_locator(d) := d.locator {
	not is_null(d.locator)
} else := ""

snapshot_accessibility_kind = "aws_rds_snapshot_accessibility"

snapshot_accessibility_header(h) = x {
	x := {
		"api_version": "decision.api.shisho.dev/v1beta",
		"kind": snapshot_accessibility_kind,
		"subject": h.subject,
		"locator": h.locator,
		"severity": h.severity,
		"labels": {},
		"annotations": {
			"decision.api.shisho.dev:aws/fsbp/latest": "RDS.1",
			"decision.api.shisho.dev:needs-manual-review": "false",
			"decision.api.shisho.dev:ssc/category": "infrastructure",
		},
		"type": shisho.decision.as_decision_type(snapshot_accessibility_allowed(h)),
	}
}

# Force to allow the given decision following resource exception policy
snapshot_accessibility_allowed(h) {
	data.params != null
	data.params.resource_exceptions != null
	shisho.resource.is_excepted(data.params.resource_exceptions, h.subject)
} else := h.allowed

# METADATA
# title: "Entry of decision.api.shisho.dev/v1beta:aws_rds_snapshot_accessibility"
# scope: "rule"
# description: |
#   Emits a decision entry describing the detail of a decision decision.api.shisho.dev/v1beta:aws_rds_snapshot_accessibility
#
#   The parameter `data` is an object with the following fields:
#   - engine: string
#   - snapshots: {"id": string, "publicly_restorable": boolean}
#
#   For instance, `data` can take the following value:
#   ```rego
#   {
#     "engine": "example",
#     "snapshots": [{"id": "example", "publicly_restorable": false}],
#   }
#   ```
snapshot_accessibility_payload(edata) := x {
	snapshot_accessibility_payload_assert(edata, "<the argument to snapshot_accessibility_payload>")
	x := json.marshal(edata)
} else := ""

snapshot_accessibility_payload_assert(edata, hint) {
	assertion.is_type(edata, "object", hint)

	key_checks := [
		assertion.has_key(edata, "engine", concat("", [hint, ".", "engine"])),
		assertion.has_key(edata, "snapshots", concat("", [hint, ".", "snapshots"])),
	]
	every c in key_checks { c }

	value_checks := [
		snapshot_accessibility_payload_assert_engine(edata, "engine", concat("", [hint, ".", "engine"])),
		snapshot_accessibility_payload_assert_snapshots(edata, "snapshots", concat("", [hint, ".", "snapshots"])),
	]
	every c in value_checks { c }
} else := false

snapshot_accessibility_payload_assert_engine(x, key, hint) {
	assertion.is_type(x[key], "string", hint)
} else := false

snapshot_accessibility_payload_assert_snapshots(x, key, hint) {
	assertion.is_set_or_array(x[key], hint)
	checks := [snapshot_accessibility_payload_assert_snapshots_element(x[key], i, concat("", [
		hint,
		"[",
		format_int(i, 10),
		"]",
	])) |
		_ := x[key][i]
	]
	every c in checks { c }
} else := false

snapshot_accessibility_payload_assert_snapshots_element(x, key, hint) {
	assertion.is_type(x[key], "object", hint)
	key_checks := [
		assertion.has_typed_key(x[key], "id", "string", hint),
		assertion.has_typed_key(x[key], "publicly_restorable", "boolean", hint),
	]
	every c in key_checks { c }
	value_checks := [
		snapshot_accessibility_payload_assert_snapshots_element_id(x[key], "id", concat("", [hint, ".", "id"])),
		snapshot_accessibility_payload_assert_snapshots_element_publicly_restorable(x[key], "publicly_restorable", concat("", [hint, ".", "publicly_restorable"])),
	]
	every c in value_checks { c }
} else := false

snapshot_accessibility_payload_assert_snapshots_element_id(x, key, hint) {
	assertion.is_type(x[key], "string", hint)
} else := false

snapshot_accessibility_payload_assert_snapshots_element_publicly_restorable(x, key, hint) {
	assertion.is_type(x[key], "boolean", hint)
} else := false

# - This code was generated by Shisho Cloud's internal system (protoc-gen-regolib).
# - When you find something to improve, you can create an GitHub issue, instead of creating pull requests.

package shisho.decision.googlecloud.asset

import data.shisho

# @title Ensure Google Cloud assets and their changes are recorded
# You can emit this decision as follows:
# 
# ```
# import data.shisho
# 
# decisions[d] {
#   # the resource ID to review (e.g. a GitHub repository, etc.)
#   subject := "test"
#
#   # whether the target is allowed by this policy or not
#   allowed := true
#
#   d := shisho.decision.googlecloud.asset.management({
#     "allowed": allowed,
#     "subject": subject,
#     "payload": shisho.decision.googlecloud.asset.management_payload({
#       "enabled": false,
#     }),
#   })
# }
# ```

# METADATA
# title: "decision.api.shisho.dev/v1beta:googlecloud_asset_management"
# scope: "rule"
# description: |
#   Emits a decision whose type is decision.api.shisho.dev/v1beta:googlecloud_asset_management".
management(d) = x {
	x := {
		"header": management_header({
			"allowed": d.allowed,
			"subject": d.subject,
			"locator": management_locator(d),
			"severity": management_severity(d),
		}),
		"payload": d.payload,
	}
}

management_severity(d) := shisho.decision.severity_info {
	d.allowed == true
} else := d.severity {
	not is_null(d.severity)
} else := 0

management_locator(d) := d.locator {
	not is_null(d.locator)
} else := ""

management_kind = "googlecloud_asset_management"

management_header(h) = x {
	x := {
		"api_version": "decision.api.shisho.dev/v1beta",
		"kind": management_kind,
		"subject": h.subject,
		"locator": h.locator,
		"severity": h.severity,
		"labels": {},
		"annotations": {
			"decision.api.shisho.dev:googlecloud/cis-benchmark/v1.3.0": "2.13",
			"decision.api.shisho.dev:needs-manual-review": "false",
			"decision.api.shisho.dev:ssc/category": "infrastructure",
		},
		"type": shisho.decision.as_decision_type(management_allowed(h)),
	}
}

# Force to allow the given decision following resource exception policy
management_allowed(h) {
	data.params != null
	data.params.resource_exceptions != null
	shisho.resource.is_excepted(data.params.resource_exceptions, h.subject)
} else := h.allowed

# METADATA
# title: "Entry of decision.api.shisho.dev/v1beta:googlecloud_asset_management"
# scope: "rule"
# description: |
#   Emits a decision entry describing the detail of a decision decision.api.shisho.dev/v1beta:googlecloud_asset_management
#
#   The parameter `data` is an object with the following fields: 
#   - enabled: boolean
#
#   For instance, `data` can take the following value:
#   ```rego
#   {
#     "enabled": false,
#   }
#   ```
management_payload(edata) = x {
	x := json.marshal(edata)
}

# - This code was generated by Shisho Cloud's internal system (protoc-gen-regolib).
# - When you find something to improve, you can create an GitHub issue, instead of creating pull requests.

package shisho.decision.googlecloud.firebaseauth

import data.shisho
import data.shisho.assertion
import data.shisho.primitive

import future.keywords.every

# @title Ensure Firebase Authentication Password Policy Is Enabled
# You can emit this decision as follows:
#
# ```
# import data.shisho
#
# decisions[d] {
#   # the resource ID to review (e.g. a GitHub repository, etc.)
#   subject := "test"
#
#   # whether the target is allowed by this policy or not
#   allowed := true
#
#   d := shisho.decision.googlecloud.firebaseauth.password_policy({
#     "allowed": allowed,
#     "subject": subject,
#     "payload": shisho.decision.googlecloud.firebaseauth.password_policy_payload({
#       "enabled_password_policy": false,
#       "tenant_scopes": [{"tenant_name": "example", "enabled_password_policy": false, "enforced_password_policy": false}],
#     }),
#   })
# }
# ```

# METADATA
# title: "decision.api.shisho.dev/v1beta:googlecloud_firebaseauth_password_policy"
# scope: "rule"
# description: |
#   Emits a decision whose type is decision.api.shisho.dev/v1beta:googlecloud_firebaseauth_password_policy".
password_policy(d) = x {
	shisho.decision.has_required_fields(d)
	x := {
		"header": password_policy_header({
			"allowed": d.allowed,
			"subject": d.subject,
			"locator": password_policy_locator(d),
			"severity": password_policy_severity(d),
		}),
		"payload": d.payload,
	}
}

password_policy_severity(d) := shisho.decision.severity_info {
	d.allowed == true
} else := d.severity {
	not is_null(d.severity)
} else := 1

password_policy_locator(d) := d.locator {
	not is_null(d.locator)
} else := ""

password_policy_kind = "googlecloud_firebaseauth_password_policy"

password_policy_header(h) = x {
	x := {
		"api_version": "decision.api.shisho.dev/v1beta",
		"kind": password_policy_kind,
		"subject": h.subject,
		"locator": h.locator,
		"severity": h.severity,
		"labels": {},
		"annotations": {
			"decision.api.shisho.dev:needs-manual-review": "true",
			"decision.api.shisho.dev:ssc/category": "infrastructure",
		},
		"type": shisho.decision.as_decision_type(password_policy_allowed(h)),
	}
}

# Force to allow the given decision following resource exception policy
password_policy_allowed(h) {
	data.params != null
	data.params.resource_exceptions != null
	shisho.resource.is_excepted(data.params.resource_exceptions, h.subject)
} else := h.allowed

# METADATA
# title: "Entry of decision.api.shisho.dev/v1beta:googlecloud_firebaseauth_password_policy"
# scope: "rule"
# description: |
#   Emits a decision entry describing the detail of a decision decision.api.shisho.dev/v1beta:googlecloud_firebaseauth_password_policy
#
#   The parameter `data` is an object with the following fields:
#   - enabled_password_policy: boolean
#   - tenant_scopes: {"tenant_name": string, "enabled_password_policy": boolean, "enforced_password_policy": boolean}
#
#   For instance, `data` can take the following value:
#   ```rego
#   {
#     "enabled_password_policy": false,
#     "tenant_scopes": [{"tenant_name": "example", "enabled_password_policy": false, "enforced_password_policy": false}],
#   }
#   ```
password_policy_payload(edata) := x {
	password_policy_payload_assert(edata, "<the argument to password_policy_payload>")
	x := json.marshal(edata)
} else := ""

password_policy_payload_assert(edata, hint) {
	assertion.is_type(edata, "object", hint)

	key_checks := [
		assertion.has_key(edata, "enabled_password_policy", concat("", [hint, ".", "enabled_password_policy"])),
		assertion.has_key(edata, "tenant_scopes", concat("", [hint, ".", "tenant_scopes"])),
	]
	every c in key_checks { c }

	value_checks := [
		password_policy_payload_assert_enabled_password_policy(edata, "enabled_password_policy", concat("", [hint, ".", "enabled_password_policy"])),
		password_policy_payload_assert_tenant_scopes(edata, "tenant_scopes", concat("", [hint, ".", "tenant_scopes"])),
	]
	every c in value_checks { c }
} else := false

password_policy_payload_assert_enabled_password_policy(x, key, hint) {
	assertion.is_type(x[key], "boolean", hint)
} else := false

password_policy_payload_assert_tenant_scopes(x, key, hint) {
	assertion.is_set_or_array(x[key], hint)
	checks := [password_policy_payload_assert_tenant_scopes_element(x[key], i, concat("", [
		hint,
		"[",
		format_int(i, 10),
		"]",
	])) |
		_ := x[key][i]
	]
	every c in checks { c }
} else := false

password_policy_payload_assert_tenant_scopes_element(x, key, hint) {
	assertion.is_type(x[key], "object", hint)
	key_checks := [
		assertion.has_typed_key(x[key], "tenant_name", "string", hint),
		assertion.has_typed_key(x[key], "enabled_password_policy", "boolean", hint),
		assertion.has_typed_key(x[key], "enforced_password_policy", "boolean", hint),
	]
	every c in key_checks { c }
	value_checks := [
		password_policy_payload_assert_tenant_scopes_element_tenant_name(x[key], "tenant_name", concat("", [hint, ".", "tenant_name"])),
		password_policy_payload_assert_tenant_scopes_element_enabled_password_policy(x[key], "enabled_password_policy", concat("", [hint, ".", "enabled_password_policy"])),
		password_policy_payload_assert_tenant_scopes_element_enforced_password_policy(x[key], "enforced_password_policy", concat("", [hint, ".", "enforced_password_policy"])),
	]
	every c in value_checks { c }
} else := false

password_policy_payload_assert_tenant_scopes_element_enabled_password_policy(x, key, hint) {
	assertion.is_type(x[key], "boolean", hint)
} else := false

password_policy_payload_assert_tenant_scopes_element_enforced_password_policy(x, key, hint) {
	assertion.is_type(x[key], "boolean", hint)
} else := false

password_policy_payload_assert_tenant_scopes_element_tenant_name(x, key, hint) {
	assertion.is_type(x[key], "string", hint)
} else := false
